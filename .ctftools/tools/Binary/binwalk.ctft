Binwalk:

Binwalk is a commandline tool which can be used to analyse binary files for hidden data.

Installation:

sudo apt install binwalk

Usage:
binwalk <file> | looks throught the file for certain "key hexvalues" for specific data
binwalk -e <file> | extracts the found data
